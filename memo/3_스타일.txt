▷ Vue 를 이용한 스타일 적용
	1) 인라인 스타일
		v-bind 를 통해 스타일을 지정할 수 있다.

	2) CSS 클래스 바인딩 (v-bind 를 이용)
		- CSS 클래스명 문자열 그대로 바인딩 하기
		- true/false 값을 가진 객체를 바인딩 하기
		- computed 를 이용한 동적 스타일 바인딩

	※ computed 속성이나 메서드가 리턴하는 값이 CSS 클래스명의 문자열이거나
	{ 클래스명 : true/false } 형태의 객체라면 손 쉽게 CSS 클래스로 바인딩할 수 있다.